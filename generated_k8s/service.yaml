apiVersion: v1
kind: Service
metadata:
  name: skyline-service
  namespace: skyline
  labels:
    app: skyline
    managed-by: amazon-q-ai
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: skyline
---