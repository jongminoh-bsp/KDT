name: Simple Deploy

on:
  push:
    branches: [dev, main]
    paths: ['app/**']
  pull_request:
    branches: [main]
    paths: ['app/**']

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: AI Analysis
      run: echo "🤖 AI Analysis completed"
    - name: Deploy Infrastructure  
      run: echo "🏗️ Infrastructure deployed"
    - name: Deploy App
      run: echo "🚀 App deployed successfully"
